# Auth.js
AUTH_SECRET=  # Generate with: openssl rand -base64 32
AUTH_URL=http://localhost:3000

# API
NEXT_PUBLIC_API_URL=http://localhost:8000
API_URL=http://localhost:8000  # Server-side (non-public)

# SAML (optional, for SSO testing)
# No additional config needed -- SAML is driven by the org's /ee/v1/saml/{slug}/login endpoint
