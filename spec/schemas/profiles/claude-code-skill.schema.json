{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://skillledger.in/schemas/v0.1/profiles/claude-code-skill.schema.json",
  "title": "Claude Code Skill Profile",
  "description": "Profile fields for Claude Code skills",
  "type": "object",
  "properties": {
    "skill_name": {
      "type": "string",
      "description": "Name of the skill as registered in Claude Code"
    },
    "triggers": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Slash commands or patterns that activate the skill"
    },
    "load_behavior": {
      "type": "string",
      "enum": ["auto", "manual", "lazy"],
      "description": "When the skill is loaded into the agent context"
    }
  },
  "additionalProperties": true
}
