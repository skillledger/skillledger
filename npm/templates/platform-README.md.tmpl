# @skillledger/cli-{PLATFORM}

Platform-specific binary package for [skillledger](https://www.npmjs.com/package/skillledger).

This package is installed automatically as an optionalDependency of `skillledger` on {PLATFORM_DISPLAY} systems. You should not install it directly.

## Platform

- OS: {OS_FIELD}
- CPU: {CPU_FIELD}
